import pickle

class PersistenceUtils:

    def write_pickle(self):
        num_of_acc_data =
        for i in range(acc_data):

    with open ('filename.pickle', 'wb') as acc_file:
        pickle.dump(data, acc_file)
        acc_file.close()

    def load_pick(self):
        with open ('filename.pickle', 'rb') as acc_file:
        pickle.load(acc_file)
        acc_file.close()

